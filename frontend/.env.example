# Copy this file to .env.local for local overrides.
# In production (Vercel), set VITE_API_URL to empty string — the
# /api/* routes are served by Vercel serverless functions on the same domain.
VITE_API_URL=
